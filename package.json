{
  "name": "shoetopia-backend",
  "version": "1.0.0",
  "description": "JSON Server API for ShoeTopia",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
